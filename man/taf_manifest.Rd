% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taf.R
\name{taf_manifest}
\alias{taf_manifest}
\title{Inventory installed TIGER Address Feature files}
\usage{
taf_manifest(year = as.character(2025:2011), version = "v2", validate = FALSE)
}
\arguments{
\item{year}{character, length 1; vintage of TIGER addrfeat (address feature)
files}

\item{version}{character, length 1; major version of the package
and taf dataset schema}

\item{validate}{logical, length one; validate every manifest row and reject
missing, untracked, unreadable, or modified Parquet files?}
}
\value{
A tibble with one row per installed county-ZIP file and columns
\code{county_fips}, \code{ZIP}, \code{zip3}, \code{zip2}, \code{n_ranges}, \code{size_bytes}, \code{sha256},
\code{taf_year}, \code{taf_version}, and \code{installed_at}.
}
\description{
\code{taf_manifest()} reads the local inventory written as county TAF files are
installed. Each row represents one installed county-ZIP Parquet file. Set
\code{validate = TRUE} to verify the manifest schema and every inventoried file,
including its storage schema, row count, byte size, and SHA-256 digest.
}
\details{
The local manifest is distinct from \code{\link[=taf_catalog]{taf_catalog()}}. The catalog describes
every county-ZIP combination available from TIGER, while the manifest records
only files installed on the current machine.
}
\examples{
\dontrun{
taf_manifest()
taf_manifest(validate = TRUE)
}
}
