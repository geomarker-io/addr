% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addr_left_join.R
\name{addr_left_join}
\alias{addr_left_join}
\title{Left join two data frames using addr matching}
\usage{
addr_left_join(
  x,
  y,
  by = "addr",
  suffix = c(".x", ".y"),
  zip_variants = TRUE,
  osa_max_dist = 1L,
  progress = interactive()
)
}
\arguments{
\item{x, y}{data frames or tibbles with an addr column}

\item{by}{addr column name in \code{x} (and \code{y} if the same); or a length-2
character vector of \code{c(x_col, y_col)}}

\item{suffix}{character vector of length 2 used to suffix duplicate columns}

\item{zip_variants}{logical; fuzzy match to common ZIP code variants in
\code{match_zipcodes()}?}

\item{osa_max_dist}{integer maximum OSA distance used by
\code{match_addr_number()}}

\item{progress}{logical; show \code{addr_match()} progress?}
}
\value{
A data frame with left-join semantics. Duplicate rows in \code{y} with
the exact same matched \code{addr} are all returned. Partial ZIP-only or
street-only matches do not expand to multiple candidate rows in \code{y}.
}
\description{
\code{addr_left_join()} is a convenience wrapper around \code{\link[=addr_match]{addr_match()}} that
returns a left-join style result. It expands rows of \code{x} for duplicate rows
in the original \code{y} that share the exact matched \code{addr}, but it does not
return multiple distinct candidate addresses from \code{y}. \code{addr_match()} still
selects a single best address before this wrapper expands exact duplicates.
}
