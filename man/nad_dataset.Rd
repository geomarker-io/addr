% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nad.R
\name{nad_dataset}
\alias{nad_dataset}
\title{Open installed National Address Database counties as an Arrow dataset}
\usage{
nad_dataset(version = 23L)
}
\arguments{
\item{version}{integer, length one; NAD revision to use. Only revision 23 is
supported.}
}
\value{
An Arrow \code{FileSystemDataset}.
}
\description{
\code{nad_dataset()} opens every installed county Parquet file as one lazy Arrow
dataset. The storage schema contains primitive columns suitable for
filtering, projection, and collection with dplyr. \code{state} and \code{county_fips}
are Hive partition columns. Use \code{nad()} when reconstructed \code{addr} and \code{s2}
columns are needed for one county.
}
\examples{
\dontrun{
nad_dataset() |>
  dplyr::filter(state == "OH", county_fips == "39017") |>
  dplyr::select(uuid, address_number, street_name, longitude, latitude) |>
  dplyr::collect()
}
}
