% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/county_fips.R
\name{county_fips_lookup}
\alias{county_fips_lookup}
\title{Translate county names and county FIPS identifiers}
\usage{
county_fips_lookup(county, state = NULL)
}
\arguments{
\item{county}{character, length one; either a county name or a 5-digit
county FIPS identifier}

\item{state}{character, length one; state abbreviation or full state name;
required when \code{county} is a name, ignored when \code{county} is already a
5-digit FIPS identifier}
}
\value{
A tibble with one row and columns \code{county}, \code{county_full}, \code{state},
and \code{county_fips}.
}
\description{
\code{county_fips_lookup()} uses a package-internal reference derived from the
2025 U.S. Census county adjacency file to translate between county names,
state abbreviations, and 5-digit county FIPS identifiers.

Name lookups accept either the full county-equivalent label
(for example, \code{"Orleans Parish"}) or a shortened form with common suffixes
removed (for example, \code{"Orleans"}). If a shortened form is ambiguous within
a state, the function errors and asks for the full county-equivalent name
or the 5-digit FIPS identifier.
}
\examples{
county_fips_lookup("Hamilton", "OH")
county_fips_lookup("Hamilton", "Ohio")
county_fips_lookup("39061")
}
